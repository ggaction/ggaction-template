# {{packageName}}

{{description}}

This repository was generated by `create-ggaction-extension`. Its living
product contract is [spec/SPEC.md](spec/SPEC.md), and its agent workflow is
defined in [AGENTS.md](AGENTS.md).

## Start

```sh
npm install
npm run spec:validate
npm test
npm run typecheck
```

Importing the package registers `{{referenceActionName}}()` on the complete
`chart()` program from ggaction:

```javascript
import { chart } from "ggaction";
import "{{packageName}}";

const program = chart()
  .createCanvas({ width: 160, height: 120 })
  .{{referenceActionName}}();
```

The reference action is an executable extension-authoring baseline. Replace or
extend it only after the living specification defines the domain feature and
its observable contract.
